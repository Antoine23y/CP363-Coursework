SELECT broad.broadDesc, COUNT(memberBroad.memberBroadMemberId) AS numMembers
FROM broad
JOIN memberBroad ON broad.broadId = memberBroad.memberBroadBroadID
GROUP BY broad.broadDesc
ORDER BY broad.broadDesc
